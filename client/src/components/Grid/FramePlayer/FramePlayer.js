import React, { Component } from 'react';

class FramePlayer extends Component {
  static propTypes = {
    frames: React.PropTypes.array,
  };
  render() {
    return (
      <div className="FramePlayer">
      </div>
    );
  }
}

export default FramePlayer;